export type AlertColors = 'danger' | 'warning' | 'success' | 'info';

export enum AlertColorClasses {
  'info' = 'color-info',
  'success' = 'color-success',
  'warning' = 'color-warning',
  'danger' = 'color-danger',
}

export enum AlertIconClasses {
  'info' = 'icon-info',
  'success' = 'icon-check-task',
  'warning' = 'icon-attention',
  'danger' = 'icon-attention',
}

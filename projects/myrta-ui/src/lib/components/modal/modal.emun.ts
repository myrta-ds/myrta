export enum ModalColorEnum {
  'default' = 'type-default',
  'info' = 'type-info',
  'attention' = 'type-attention',
  'negative' = 'type-negative',
}

export type ModalColorTypes = 'default' | 'info' | 'attention' | 'negative'

export enum ModalSizesEnum {
  'small' = 'mrx-modal-sm',
  'medium' = 'mrx-modal-md',
  'large' = 'mrx-modal-lg',
  'extra-large' = 'mrx-modal-extra-lg'
}

export type ModalSizesTypes = 'small' | 'medium' | 'large' | 'extra-large'

export enum ModalAlignButtonsEnum {
  'center' = 'justify-content-center',
  'left' = 'justify-content-start',
  'right' = 'justify-content-end',
}

export type ModalAlignButtonsTypes = 'center' | 'left' | 'right'

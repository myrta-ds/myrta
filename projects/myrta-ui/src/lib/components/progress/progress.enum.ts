export type ProgressColors = 'default' | 'success' | 'warning' | 'danger';
export enum ProgressClasses {
  'default' = 'mrx-progress-default',
  'success' = 'mrx-progress-success',
  'warning' = 'mrx-progress-warning',
  'danger' = 'mrx-progress-danger',
}
